{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the launcher window",
  "windows": ["launcher"],
  "permissions": ["core:default"]
}
